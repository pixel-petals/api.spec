$kind: http-request
url: "{{baseUrl}}/api/0.6/way/:id/full"
method: GET
headers:
  Accept: application/xml
pathVariables:
  - key: id
    value: "6776"
    description: The numeric ID of the element
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "format": "int64"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
auth:
  type: noauth
examples: ./.resources/Get a way and all its nodes.resources/examples
order: 1000
