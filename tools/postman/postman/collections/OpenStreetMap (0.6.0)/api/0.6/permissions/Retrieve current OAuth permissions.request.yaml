$kind: http-request
description: Returns the permissions granted to the current API connection. Returns an empty list if not authenticated.
url: "{{baseUrl}}/api/0.6/permissions"
method: GET
headers:
  Accept: application/xml
body:
  type: text
  content: ""
auth:
  type: noauth
examples: ./.resources/Retrieve current OAuth permissions.resources/examples
order: 1000
