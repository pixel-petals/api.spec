$kind: http-request
url: "{{CKAN_API}}/group_update"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the name or id of the group to update
order: 8000
