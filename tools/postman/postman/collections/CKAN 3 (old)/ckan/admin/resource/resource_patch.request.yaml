$kind: http-request
url: "{{CKAN_API}}/resource_patch"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the id of the resource
order: 5000
