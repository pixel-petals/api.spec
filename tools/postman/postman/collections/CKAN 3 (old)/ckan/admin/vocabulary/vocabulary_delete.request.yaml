$kind: http-request
url: "{{CKAN_API}}/vocabulary_delete"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the id of the vocabulary
order: 3000
