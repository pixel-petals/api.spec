$kind: http-request
url: "{{CKAN_API}}/organization_member_delete"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the id or name of the organization
    - type: text
      key: username
      value: ""
      description: (string) – name or id of the user to be removed
order: 4000
