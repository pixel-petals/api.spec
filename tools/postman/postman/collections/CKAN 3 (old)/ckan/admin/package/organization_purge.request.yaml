$kind: http-request
url: "{{CKAN_API}}/organization_purge"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the name or id of the organization to be purged
order: 4000
