$kind: http-request
url: "{{CKAN_API}}/package_owner_org_update"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the name or id of the dataset to update
    - type: text
      key: organization_id
      value: ""
      description: the name or id of the owning organization
order: 9000
