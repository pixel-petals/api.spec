$kind: http-request
url: "{{CKAN_API}}/package_resource_reorder"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the id or name of the package to update
    - type: text
      key: order
      value: ""
      description: a list of resource ids in the order needed
order: 11000
