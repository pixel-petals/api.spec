$kind: http-request
url: "{{CKAN_API}}/group_followee_list?id"
method: GET
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
queryParams:
  - key: id
    value: null
    description: (string) – the id or name of the user
order: 9000
