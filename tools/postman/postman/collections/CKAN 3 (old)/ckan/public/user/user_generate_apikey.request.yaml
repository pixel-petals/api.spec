$kind: http-request
url: "{{CKAN_API}}/user_generate_apikey"
method: POST
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
body:
  type: formdata
  content:
    - type: text
      key: id
      value: ""
      description: (string) – the name or id of the user whose key needs to be updated
order: 22000
