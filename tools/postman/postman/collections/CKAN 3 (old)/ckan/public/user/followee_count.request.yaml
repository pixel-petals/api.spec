$kind: http-request
url: "{{CKAN_API}}/followee_count?id"
method: GET
headers:
  Content-Type: application/json
  Authorization: "{{api-key}}"
queryParams:
  - key: id
    value: null
    description: (string) – the id of the user
order: 8000
