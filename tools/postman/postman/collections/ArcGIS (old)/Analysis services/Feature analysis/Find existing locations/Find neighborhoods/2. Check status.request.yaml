$kind: http-request
description: Checks the status of a FindExistingLocations analysis job using the provided job ID. Use this endpoint to monitor the progress and retrieve the current state of your submitted FindExistingLocations job.
url: "{{ANALYSIS_URL}}/FindExistingLocations/jobs/{{JOB_ID}}"
method: POST
body:
  type: urlencoded
  content:
    f: json
    token: "{{ACCESS_TOKEN}}"
examples: ./.resources/2. Check status.resources/examples
order: 2000
