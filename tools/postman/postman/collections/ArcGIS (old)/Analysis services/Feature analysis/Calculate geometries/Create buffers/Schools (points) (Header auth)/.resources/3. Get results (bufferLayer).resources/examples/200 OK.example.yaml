$kind: http-example
request:
  url: "{{ANALYSIS_URL}}/CreateBuffers/jobs/{{job_id}}/results/bufferLayer"
  method: POST
  body:
    type: urlencoded
    content:
      f: json
      returnType: data
      token: "{{ACCESS_TOKEN}}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: |-
      {
        "paramName": "bufferLayer",
        "dataType": "GPString",
        "value": {
            "url": "<SERVICE_URL>",
            "itemId": "<ITEM_ID>"
        }
      }
order: 1000
