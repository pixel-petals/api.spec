$kind: collection
description: To learn more go to the [Developer guide](https://developers.arcgis.com/documentation/mapping-and-location-services/routing-and-directions/vehicle-routing-problem/
scripts:
  - type: http:beforeRequest
    code: |-
      // utils.requiredParameters(['f', 'token']);
      // utils.checkSupportedValues("f", ["json", "pjson"]);
    language: text/javascript
order: 3000
