$kind: http-request
description: Export the full dataset as a CSV file. Other formats are also available (e.g. export.json, export.tsv).
url: "{{baseUrl}}/api/v3/views/{{datasetId}}/export.csv"
method: GET
headers:
  X-App-Token: "{{appToken}}"
order: 1000
