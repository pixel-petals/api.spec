{
  "name": "f",
  "in": "query",
  "description": "Response format. `json` (default), `pjson` (pretty-JSON), `geojson`, `html`, `xml`.",
  "schema": {
    "type": "string",
    "enum": [
      "json",
      "pjson",
      "geojson",
      "html",
      "xml",
      "csv",
      "kmz"
    ],
    "default": "json"
  }
}