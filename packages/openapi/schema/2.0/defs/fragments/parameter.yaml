$schema: http://json-schema.org/draft-04/schema#
if:
  type: object
  required:
    - $ref
then:
  $ref: ./response.yaml
else:
  oneOf:
    - $ref: ../parameter/bodyParameter.yaml
    - $ref: ../parameter/nonBodyParameter.yaml
