$schema: https://json-schema.org/draft/2020-12/schema
type: array
items:
  $ref: ../components/parameters.yaml
not:
  allOf:
    - contains:
        type: object
        properties:
          in:
            const: query
        required:
          - in
    - contains:
        type: object
        properties:
          in:
            const: querystring
        required:
          - in
contains:
  type: object
  properties:
    in:
      const: querystring
  required:
    - in
minContains: 0
maxContains: 1
