$schema: https://json-schema.org/draft/2020-12/schema
description: Used by the client to get a prompt provided by the server.
properties:
  id:
    $ref: ../fragments/requestId.yaml
  jsonrpc:
    const: "2.0"
    type: string
  method:
    const: prompts/get
    type: string
  params:
    $ref: ../fragments/getPromptRequestParams.yaml
required:
  - id
  - jsonrpc
  - method
  - params
type: object
